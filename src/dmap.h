#ifndef TETRIS_DMAP_H
#define TETRIS_DMAP_H

#include "common.h"
#include <vector>

using namespace std;

extern const vector<pair<int, int> > dMap[28];
extern int height[28];

#endif //TETRIS_DMAP_H
